using UnityEngine;

namespace Content.Scripts.SnowSimulation
{
    public class SnowSaturation : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
